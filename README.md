# Реализация интерпретатора конфигурационного файла

Принимает на вход конфигурационный файл в формате `json`.

Дополнительная установка пакетов

```
conda install jsonschema
```

Запуск

```
python scheduler.py 
```

